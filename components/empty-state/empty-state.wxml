<view class="empty-container">
  <image class="empty-icon" src="{{image || '/images/empty-cart.png'}}" mode="aspectFit"></image>
  <view class="empty-text">{{description}}</view>
  <button wx:if="{{showButton}}" class="empty-btn" bindtap="onTap">{{buttonText}}</button>
</view>
